# 🧪 СТРАТЕГИЯ ТЕСТИРОВАНИЯ ВИДЖЕТА amoCRM

## 📋 ПЛАН ДЕЙСТВИЙ

### ШАГ 1: Тест минимального виджета

```powershell
cd D:\табель
python create_minimal_archive.py
```

**Результат:** `widget_minimal_test.zip`

**Что внутри:**
- `manifest.json` - минимальная конфигурация
- `script.js` - пустые callbacks
- `i18n/ru.json` - базовые ключи
- `images/tour_ru.png` - одно изображение

---

### ШАГ 2: Загрузите минимальный виджет

1. Откройте amoCRM
2. **Настройки** → **амоМаркет** → **Создать интеграцию**
3. Загрузите `widget_minimal_test.zip`

**Ожидаемый результат:**
- ✅ Виджет загружается БЕЗ ошибок
- ✅ Нет ошибки "Required /i18n/ru.json"
- ✅ Нет ошибки "Failed to get object manifest.json"

---

### ШАГ 3: Если минимальный работает

```powershell
python create_widget_archive.py
```

**Результат:** `widget.zip` (полная версия)

Загрузите его в amoCRM!

---

## 🔍 ЧТО ПОКАЗЫВАЕТ СКРИПТ

Скрипт выведет структуру архива:

```
[OK] Adding: manifest.json
[OK] Adding: script.js
[OK] Adding: i18n/ru.json
[OK] Adding: images/tour_ru.png

SUCCESS! Created: widget_minimal_test.zip
Size: 2.45 KB

Files in archive:
  - manifest.json
  - script.js
  - i18n/ru.json
  - images/tour_ru.png
```

**Проверьте:** 
- ✅ Пути используют `/` (не `\`)
- ✅ Файлы в корне архива (не в подпапке)
- ✅ Путь к i18n: `i18n/ru.json` (не `/i18n/ru.json`)

---

## ❌ ЕСЛИ МИНИМАЛЬНЫЙ НЕ РАБОТАЕТ

### Вариант A: Ошибка "/i18n/ru.json not found"

**Причина:** Архив создан неправильно

**Решение:** 
1. Откройте `widget_minimal_test.zip` в 7-Zip/WinRAR
2. Проверьте структуру - файлы должны быть в КОРНЕ
3. НЕ должно быть папки `widget_minimal/` внутри архива!

### Вариант B: Ошибка "Failed to get manifest.json"

**Причина:** manifest.json поврежден или в неправильной кодировке

**Решение:**
1. Проверьте кодировку: должна быть UTF-8 без BOM
2. Проверьте валидность JSON на https://jsonlint.com/

### Вариант C: Другие ошибки

**Напишите сюда полный текст ошибки** - разберемся!

---

## ✅ ЕСЛИ МИНИМАЛЬНЫЙ РАБОТАЕТ

Это значит:
1. ✅ Структура архива правильная
2. ✅ Python скрипт работает корректно
3. ✅ amoCRM принимает архив

**Переходим к полному виджету:**

```powershell
python create_widget_archive.py
```

Загружаем `widget.zip` в amoCRM!

---

## 🎯 ДИАГНОСТИКА ПО ШАГАМ

| Шаг | Результат | Действие |
|-----|-----------|----------|
| Минимальный работает | ✅ | → Загружаем полный виджет |
| Минимальный НЕ работает | ❌ | → Проверяем структуру архива |
| Полный работает | ✅ | → ГОТОВО! 🎉 |
| Полный НЕ работает | ❌ | → Ищем проблему в коде |

---

## 📝 ЧЕКЛИСТ ПЕРЕД ЗАГРУЗКОЙ

### Минимальный виджет:
- [ ] Запущен: `python create_minimal_archive.py`
- [ ] Создан файл: `widget_minimal_test.zip`
- [ ] Размер: ~2-3 KB
- [ ] Структура проверена в архиваторе

### Полный виджет:
- [ ] Запущен: `python create_widget_archive.py`
- [ ] Создан файл: `widget.zip`
- [ ] Размер: ~10-15 KB
- [ ] Все 8 файлов в архиве

---

## 🚀 НАЧИНАЕМ!

```powershell
cd D:\табель
python create_minimal_archive.py
```

**Удачи!** 🍀
